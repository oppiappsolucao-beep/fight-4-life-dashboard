# Desenvolvimento local (usa o proxy do Vite)
VITE_API_URL=/api
VITE_TENANT_SLUG=oppifit

# Domínio base do SaaS — usado para montar URL da academia
VITE_APP_BASE_DOMAIN=oppifit.com.br

# Produção (exemplo): aponte para a URL pública da API + /api
# VITE_API_URL=https://academia.oppifit.com.br/api
