# Deploy Node (API + front) no EasyPanel via Nixpacks.
# Não defina NODE_ENV=production em [variables]: o npm install
# pularia vite/typescript/prisma (estão em devDependencies) e o build quebra.
# Sem imagem nova o painel fica em: No such image: easypanel/dashboard/academia:latest
providers = ["node"]

[variables]
HOST = "0.0.0.0"
WEB_DIST_PATH = "apps/web/dist"
DATABASE_URL = "postgresql://build:build@127.0.0.1:5432/build?schema=public"
DIRECT_URL = "postgresql://build:build@127.0.0.1:5432/build?schema=public"

[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]
aptPkgs = ["openssl"]

[phases.install]
cmds = ["npm install --include=dev"]

[phases.build]
cmds = [
  "npm run db:generate -w @oppi/api",
  "npm run build -w @oppi/web",
  "npm run build -w @oppi/api"
]

[start]
cmd = "node apps/api/dist/index.js"
